'use strict';
var app = require('../../server/server');
module.exports = function (Utilisateur) {
  /*
  Utilisateur.afterRemote('login', function(ctx, next) {
    var userlog=app.models.userlog;
    userlog.create({userFirstName: 'foo@bar.com',userSirName: 'foo@bar.com', userEmail: 'foo@bar.com', logindatetime: Date(), actionType: 'login'},  function(err, userInstance) {
      console.log(err,userInstance);
    });
     });
     */
};