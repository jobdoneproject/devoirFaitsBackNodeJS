'use strict';
var app = require('../../server/server');
module.exports = function (Utilisateur) {
/*
  Utilisateur.afterRemote('login', async function(ctx, user, next) {
    var userlog=app.models.userlog;
    userlog.create({user.nom,'foo@bar.com', 'foo@bar.com', Date(), 'login'},  function(err, userInstance) {
      console.log(err,user);

      });

     });*/
};